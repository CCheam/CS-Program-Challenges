#include <iostream>
#include <iomanip>
int main() {
  int weight, distance;
  double rate;
  std::cout<<"Weight of package(in kg): "<<std::endl;
  std::in<<weight;
  std::cout<<"Distance to be shipped: "<<std::endl;
  std::in<<distance;
  if(){}
    return 0;
}